# Deploy

## Build
```bash
npm run build
```
Outputs to `dist/`.

## Static host
* Vercel / Netlify: drop `dist/` as the publish directory.
* GitHub Pages: enable Pages from `gh-pages` branch.

## First-run video
* 60 seconds max
* Show menu → garage → race → results → upgrade loop
* Record at 1080p 60fps

## Checklist
- [ ] Production build passes
- [ ] Public URL live
- [ ] Video uploaded
- [ ] README updated with URL
